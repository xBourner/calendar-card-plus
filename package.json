{
  "name": "calendar-card-plus",
  "version": "1.1.0",
  "description": "Standalone Calendar Card extracted from Dynamic Grid Card",
  "scripts": {
    "dev": "vite",
    "build": "npx tsc && npx vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "lit": "^3.1.0"
  },
  "devDependencies": {
    "typescript": "^5.3.3",
    "vite": "^5.0.10"
  }
}
